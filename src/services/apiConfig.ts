// GuardConnect API Configuration

export const API_CONFIG = {
  // Replace with your backend server URL once provided by backend team
  BASE_URL: 'https://api.yourdomain.com/api',
  TIMEOUT: 10000,
  // Toggle to true for local testing with fallback data when backend is not live
  USE_MOCK_FALLBACK: true,
};
